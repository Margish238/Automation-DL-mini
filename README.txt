>>first create an enviroment:
    python -m venv env
>>then install all packages:
    pip install gradio torchvision torch selenium pyautogui pillow
>>after checking all imports works well run the code app.py:
    python app.py


##Purpose of components##
gradio:to create UI and backend functions
torch: To access open-source Deeplearning 
torchvision: for computing/trasform images 
pillow: for images
requests:this library allows to sending HTTP requests in python
selenium,pyautogui: for automation
resnet18:is modal for image classification
